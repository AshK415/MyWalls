import 'package:flutter/material.dart';

class EErrorWidget extends StatelessWidget {
  const EErrorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
